***

# 注意力压缩算法AttnZip算法文档

## 1. 引言与设计哲学
标准 Transformer 的自注意力机制（Self-Attention）具有 $O(L^2)$ 的计算和内存复杂度，这成为了长上下文建模的主要瓶颈。为了解决这一问题，学术界提出了大量线性注意力、状态空间模型（SSM）以及带有复杂门控和卷积机制的变体。然而，这些方法往往引入了大量工程补丁（如时变门控、短程卷积、Delta Rule 等），导致算法表达式臃肿，失去了 Transformer 原本“大道至简”的数学美感。

**AttnZip** 旨在回归极简主义。本算法摒弃了所有复杂的门控和状态递推机制，仅通过引入一组可学习的“记忆查询向量（Memory Queries）”，利用标准 Softmax 的归一化特性来分配注意力预算，从而将长上下文无损或有损地压缩为少量的“信息 Token”。该算法在保持表达式极度简洁的同时，实现了针对长上下文长度的线性复杂度，并完美兼容 FlashAttention 等硬件加速技术。

## 2. 核心动机
1. **拒绝“缝合怪”设计**：不使用 LSTM 式的门控或 Mamba 式的复杂状态更新，保持核心算子仅为标准的矩阵乘法与 Softmax。
2. **Softmax 即重要性分配器**：Softmax 的本质是概率分布（行和为 1）。如果引入 $M$ 个查询向量，模型就天然获得了 $M$ 个“注意力预算”。模型会自动将高权重分配给重要的 Token，将低权重分配给无用信息，从而实现基于重要性的信息压缩。
3. **隐变量而非显式文本**：压缩后的信息 Token 是连续的隐变量（Latent Variables），作为全局记忆供后续网络读取，无需在中间步骤还原为离散文本，避免了自编码器带来的额外计算开销和信息损失。

## 3. 算法公式与流程

### 3.1 符号定义
- $L$：真实长上下文的序列长度。
- $M$：压缩后的记忆 Token 数量（Bottleneck Size），且 $M \ll L$。
- $W$：局部窗口大小（Local Window Size）。
- $d$：模型的隐藏层维度。
- $X \in \mathbb{R}^{L \times d}$：输入的长上下文隐藏状态。
- $P \in \mathbb{R}^{M \times d}$：**可学习的记忆查询矩阵**（Memory Queries），这是本算法唯一引入的额外参数。

### 3.2 阶段一：全局上下文的重要性压缩
在此阶段，模型将长度为 $L$ 的上下文压缩为长度为 $M$ 的信息 Token。

首先，计算长上下文的 Key 和 Value：
$$ K = X W_K, \quad V = X W_V $$
其中 $K, V \in \mathbb{R}^{L \times d}$。

接着，使用可学习的记忆查询矩阵 $P$ 作为 Query，对 $K$ 和 $V$ 进行交叉注意力（Cross-Attention）计算：
$$ A = \text{Softmax}\left(\frac{P K^T}{\sqrt{d}}\right) \in \mathbb{R}^{M \times L} $$
$$ C = A V \in \mathbb{R}^{M \times d} $$

**原理解析**：
矩阵 $A$ 的每一行都是一个和为 1 的概率分布。$M$ 行的总权重和为 $M$。这相当于模型在 $L$ 个 Token 中分配了 $M$ 个“注意力预算”。重要性高的 Token 会获得更大的注意力权重，其 Value 被更多地聚合到 $C$ 中；无用信息则被忽略。$C$ 即为压缩后的**信息 Token（Memory Tokens）**。

### 3.3 阶段二：局部-全局联合注意力
在后续的 Transformer 层中（或文本生成阶段），当前 Token 需要同时利用全局记忆 $C$ 和局部细节 $X_{\text{local}}$。

设当前局部输入的隐藏状态为 $Y \in \mathbb{R}^{W \times d}$（$W$ 为局部窗口或当前生成长度）。
计算当前输入的 Query、Key、Value：
$$ Q_y = Y W_Q, \quad K_y = Y W_K, \quad V_y = Y W_V $$

计算全局记忆的 Key 和 Value（可直接复用阶段一的 $C$ 经过线性变换，或重新计算）：
$$ K_c = C W_K, \quad V_c = C W_V $$

将全局记忆与局部上下文在序列维度上拼接：
$$ K_{\text{cat}} = [K_c, K_y] \in \mathbb{R}^{(M+W) \times d} $$
$$ V_{\text{cat}} = [V_c, V_y] \in \mathbb{R}^{(M+W) \times d} $$

执行标准自注意力：
$$ O = \text{Softmax}\left(\frac{Q_y K_{\text{cat}}^T}{\sqrt{d}}\right) V_{\text{cat}} \in \mathbb{R}^{W \times d} $$

## 4. 复杂度与显存分析

### 4.1 计算复杂度 (FLOPs)
- **阶段一（压缩）**：计算 $P K^T$ 和 $A V$，复杂度为 $O(M \cdot L \cdot d)$。由于 $M$ 是常数且 $M \ll L$，此项对 $L$ 呈**线性复杂度** $O(L)$。
- **阶段二（联合注意力）**：计算 $Q_y K_{\text{cat}}^T$，复杂度为 $O(W \cdot (M+W) \cdot d)$。由于 $M$ 和 $W$ 均远小于 $L$，此项对 $L$ 呈**常数复杂度** $O(1)$。
- **总体计算复杂度**：$O(M \cdot L \cdot d + W \cdot (M+W) \cdot d)$。在处理超长上下文时，复杂度完全由 $O(L)$ 主导，彻底消除了 $O(L^2)$ 的瓶颈。

### 4.2 内存/显存复杂度
- 传统 Attention 需要实例化 $L \times L$ 的注意力矩阵，显存为 $O(L^2)$。
- 本算法中，阶段一的注意力矩阵大小为 $M \times L$，阶段二为 $W \times (M+W)$。
- 结合 **FlashAttention** 技术，算法在计算时通过分块（Tiling）和重计算（Recomputation）避免在 HBM（高带宽内存）中显式实例化这些注意力矩阵。
- **峰值显存**：仅需保存 $K, V$ 的激活值（$O(L \cdot d)$）以及极小的注意力分块，显存开销对 $L$ 呈**线性**，完美支持百万级上下文。

## 5. 信息流转：从隐变量到文本生成
一个常见的疑问是：*“信息 Token $C$ 压缩了上下文，它如何被还原成文本输出？”*

**答案是：信息 Token 不需要被还原成文本。**

1. **隐变量本质**：$C$ 是一组连续的高维向量，代表模型的“长期工作记忆”。它不是离散的文字，而是语义和事实的稠密表示。
2. **文本生成路径**：文本的生成由模型最后的语言模型头（LM Head）完成。在自回归生成时，Decoder 的当前隐藏状态 $h_t$ 会通过阶段二的注意力机制读取 $C$。
3. **输出映射**：融合了全局记忆 $C$ 的隐藏状态 $h_t$ 经过 FFN 和 LayerNorm 后，直接通过线性层映射到词表空间：
   $$ \text{logits}_t = h_t W_{\text{vocab}} $$
   $$ p(y_t) = \text{Softmax}(\text{logits}_t) $$
   
换言之，$C$ 只是为文本生成提供了**条件（Condition）** 和**上下文（Context）**。模型根据 $C$ 中的记忆直接组织语言输出答案，无需经历“压缩 -> 还原原文 -> 生成答案”的冗余过程。

## 6. 算法优势与局限性分析

### 6.1 核心优势
1. **极致的数学美感**：核心算子仅为标准 Attention，无门控、无卷积、无复杂递推，代码实现极简，易于理解和调试。
2. **硬件友好**：完全兼容 FlashAttention、PagedAttention 等现有底层优化，无需编写复杂的自定义 CUDA Kernel。
3. **可解释性强**：通过可视化阶段一的注意力矩阵 $A$，可以清晰地观察到模型认为长上下文中哪些 Token 是“重要”的，具备白盒特性。
4. **灵活的容量控制**：通过调整 $M$ 的大小，可以在“记忆容量”和“计算开销”之间进行平滑的 Trade-off。

### 6.2 局限性与应对
1. **有损压缩**：由于 $M \ll L$，极端长尾的细节（如“第 83721 个词是什么”）可能会在压缩中丢失。
   - *应对*：保留局部窗口 $W$（如最近 4k Token 不压缩），确保短期精确信息不丢失。
2. **重要性评估的滞后性**：在阶段一压缩时，模型可能无法预知未来问题需要哪些细节。
   - *应对*：在多层 Transformer 中，可以每隔几层重复执行一次阶段一的压缩，让不同层级的 $P$ 学习不同抽象层级的记忆。

## 7. 总结
AttnZip 证明了：在追求长上下文 $O(L)$ 复杂度的道路上，并不一定要依赖复杂的门控和状态递推。通过回归 Softmax 的归一化本质，利用可学习的 Memory Queries 进行重要性压缩，我们完全可以在保持 Transformer 极简美学的前提下，实现高效的长上下文建模。