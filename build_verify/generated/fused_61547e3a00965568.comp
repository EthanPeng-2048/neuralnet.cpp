// ── 自动生成（AOT 算子融合），请勿手动编辑 ──
// 表达式: fused_61547e3a00965568
#version 450

layout(local_size_x = 256) in;

layout(std430, binding = 0) readonly buffer Buf0 { float b0[]; };
layout(std430, binding = 1) readonly buffer Buf1 { float b1[]; };
layout(std430, binding = 2) readonly buffer Buf2 { float b2[]; };
layout(std430, binding = 3) readonly buffer Buf3 { float b3[]; };
layout(std430, binding = 4) readonly buffer Buf4 { float b4[]; };
layout(std430, binding = 5) writeonly buffer BufOut { float bout[]; };

layout(push_constant) uniform PC {
    uint count;
    uint cols;
    float c0;
    float c1;
};

void main()
{
    const uint i = gl_GlobalInvocationID.x;
    if (i >= count) return;
    const uint row = i / cols;
    const uint col = i % cols;
    const float v0 = b0[i];
    const float v1 = b1[i];
    const float v2 = b2[i];
    const float v3 = b3[i];
    const float v4 = b4[i];
    float r0 = v0 * v1;
    float r1 = c1 - v4;
    float r2 = v3 * r1;
    float r3 = c0 + r2;
    float r4 = v2 * r3;
    float r5 = r0 * r4;
    bout[i] = r5;
}
